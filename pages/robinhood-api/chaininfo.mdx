import { GetStarted } from "../../components/GenericMethod/getStarted/GetStarted";
import { Space } from "@mantine/core";
import { Meta } from "../../components/Meta";


<Meta
  title="Robinhood Chain info | dRPC"
/>

# Chain info

The Chain info group supplies details about the network, its protocol version, and its configuration. Key methods include:

[eth_chainId](/robinhood-api/chaininfo/eth_chainId) : Retrieves the chain ID of the network. \
[eth_protocolVersion](/robinhood-api/chaininfo/eth_protocolVersion) : Returns the protocol version used by the client. \
[net_listening](/robinhood-api/chaininfo/net_listening) : Checks if the client is actively listening for network connections. \
[net_version](/robinhood-api/chaininfo/net_version) : Returns the current network version. \
[net_peerCount](/robinhood-api/chaininfo/net_peerCount) : Retrieves the number of active peers. \
[eth_syncing](/robinhood-api/chaininfo/eth_syncing) : Indicates the synchronization status of the node. \
[eth_hashrate](/robinhood-api/chaininfo/eth_hashrate) : Provides the current mining hashrate of the network.

These methods provide developers with essential tools for chain info on the Robinhood network.

<Space h={16} />
<GetStarted />
